import React, { use } from 'react';

const Book = ({singleData}) => {
    // const data = use(dataPromised)
    // console.log(singleData)
    return (
        <div>
            
        </div>
    );
};

export default Book;